#include<stdio.h>
#include <string.h>
int main(){
char stringa[10];
printf("inserisci la stringa: ");
gets(stringa);
printf("la stringa è: %s",str);
printf("\n stringa in maiuscolo: %s",strupr(stringa));
return 0;
}
